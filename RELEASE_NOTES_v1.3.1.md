Tuoz1 Bot 的客户端插件。运行后自动读取英雄联盟客户端的战绩，把海克斯大乱斗等 Riot API 拿不到的模式发给机器人播报。

**v1.3.1 更新**
- 赌局开盘时能正确列出同队队友作为 MVP / 战犯候选（v1.3.0 只列出自己）。
- 令牌失效时启动会直接让你粘贴新令牌，不用删配置重来。

**v1.3.0 更新**
- 开局通知：进入对局时把比赛 ID 和同队队友告诉机器人，机器人会在播报频道开赌局，语音里的人可以用金币押 胜 / 负 / MVP / 战犯，打完自动结算。

已经在运行的旧版本会自动更新，不用手动下载。

**使用方法**
1. 在 Discord 里先用 `/bind` 绑定账号，再输入 `/companion_token` 获取机器人地址和令牌。
2. 双击运行 `Tuoz1Companion.exe`，按提示粘贴地址和令牌（只需输入一次）。
3. 打游戏时保持插件窗口开着，打完一局会自动上报；和平时一样，待在语音频道里才会播报。

程序没有数字签名，Windows 可能提示"未知发布者"，点"仍要运行"即可。令牌请勿分享给他人。
